apiVersion: v1
kind: Service
metadata:
  name: bitcoind
  namespace: crypto-demo
spec:
  type: ClusterIP
  selector:
    app: bitcoind
  ports:
    - name: rpc
      port: 8332
      targetPort: 8332
    - name: p2p
      port: 8333
      targetPort: 8333
