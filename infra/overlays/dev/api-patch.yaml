apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: crypto-demo
spec:
  template:
    spec:
      containers:
        - name: api
          env:
            - name: NETWORK
              value: regtest
